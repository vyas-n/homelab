[project]
name = "deploy"
version = "0.0.0"
description = ""
authors = [{ name = "Vyas Nellutla", email = "me@vyas-n.com" }]
requires-python = ">=3.12"
readme = "README.md"

[dependency-groups]
dev = [
    "ansible-core",
    "ansible-lint ; platform_system != 'Windows'",
]

[tool.uv]
package = false
python-preference = "only-managed"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
