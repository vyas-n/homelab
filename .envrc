#!/usr/bin/env sh

dotenv

echo "${DIGITALOCEAN_TOKEN:?Digital Ocean API Token is not set. Terraform cannot function without it.}" >/dev/null

echo "${CLOUDFLARE_API_TOKEN:?Cloudflare API Token is not set. Terraform cannot function without it.}" >/dev/null
