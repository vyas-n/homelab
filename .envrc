#!/usr/bin/env sh

dotenv

echo "${DIGITALOCEAN_TOKEN:?Digital Ocean API Token is not set. Terraform cannot function without it.}" >/dev/null

TF_VAR_cloudflare_api_token="${CLOUDFLARE_API_TOKEN:?Cloudflare API Token is not set. Terraform cannot function without it.}"
export TF_VAR_cloudflare_api_token
